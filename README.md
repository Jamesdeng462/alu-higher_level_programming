# python-hello_world

This directory contains a simple shell script that executes a Python file. The Python filename is passed via an environment variable named `$PYFILE`.

# File: 0-run

A shell script that runs a Python script defined in the environment variable `PYFILE`.


